# Сайт-визитка на GitHub Pages

Профессиональный и современный сайт-визитка для размещения на GitHub Pages.

## 🚀 Особенности

- ✨ **Современный дизайн** - минималистичный и привлекательный интерфейс
- 🌓 **Темная/Светлая тема** - автоматическое определение системных настроек + ручное переключение
- 📱 **Полностью адаптивный** - отлично работает на всех устройствах
- 🎨 **Плавные анимации** - Intersection Observer для анимаций при скролле
- ⚡ **Оптимизированная производительность** - быстрая загрузка, оптимизированный код
- 🔍 **SEO-friendly** - правильные мета-теги и семантическая разметка
- 🖨️ **Стили для печати** - оптимизированное отображение при печати резюме

## 📁 Структура проекта

```
.
├── index.html          # Главная страница
├── data.json          # Данные резюме в JSON формате
├── resume.html        # Исходное резюме
├── _config.yml        # Конфигурация Jekyll/GitHub Pages
├── css/
│   ├── styles.css     # Основные стили
│   └── themes.css     # Темы (светлая/темная)
├── js/
│   ├── data.js        # Загрузка данных резюме
│   ├── main.js        # Основная логика приложения
│   └── animations.js  # Анимации и эффекты
└── parse_resume.py    # Скрипт для парсинга резюме (опционально)
```

## 🛠️ Установка и настройка

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/aleksey512/aleksey512.github.io.git
cd aleksey512.github.io
```

2. Откройте `index.html` в браузере или используйте локальный сервер:
```bash
# Python 3
python3 -m http.server 8000

# Node.js (http-server)
npx http-server
```

3. Откройте в браузере: `http://localhost:8000`

### Развертывание на GitHub Pages

1. Убедитесь, что репозиторий называется `username.github.io` (где `username` - ваш GitHub username)

2. Загрузите файлы в репозиторий:
```bash
git add .
git commit -m "Initial commit: Professional portfolio website"
git push origin master
```

3. GitHub Pages автоматически развернет сайт через несколько минут

4. Ваш сайт будет доступен по адресу: `https://username.github.io`

## 📝 Обновление данных

### Способ 1: Редактирование data.json

Отредактируйте файл `data.json` напрямую, обновив нужные поля.

### Способ 2: Использование скрипта парсинга

Если у вас есть обновленное `resume.html`:

```bash
python3 parse_resume.py
```

Скрипт автоматически извлечет данные и обновит `data.json`.

## 🎨 Кастомизация

### Изменение цветовой схемы

Отредактируйте переменные в `css/themes.css`:

```css
:root {
    --accent-primary: #6366f1;  /* Основной цвет */
    --accent-secondary: #8b5cf6; /* Вторичный цвет */
    /* ... */
}
```

### Добавление фотографии

1. Добавьте ваше фото в корневую директорию как `photo.jpg` (или другое имя)
2. Обновите `index.html`, заменив `.image-placeholder` на:
```html
<img src="photo.jpg" alt="Алексей Якунин" class="hero-photo">
```

### Изменение шрифтов

В `index.html` измените ссылку на Google Fonts или используйте другие шрифты.

## 📱 Адаптивность

Сайт полностью адаптивен и оптимизирован для:
- 📱 Мобильных устройств (320px+)
- 📱 Планшетов (768px+)
- 💻 Десктопов (1024px+)
- 🖥️ Больших экранов (1440px+)

## 🔧 Технологии

- **HTML5** - семантическая разметка
- **CSS3** - современные возможности (Grid, Flexbox, Custom Properties)
- **JavaScript (ES6+)** - ванильный JS без фреймворков
- **Font Awesome** - иконки
- **Google Fonts** - типографика (Inter, JetBrains Mono)

## 📄 Лицензия

Этот проект создан для личного использования. Вы можете свободно использовать его как основу для своего портфолио.

## 🤝 Поддержка

Если у вас возникли вопросы или предложения, создайте Issue в репозитории.

---

**Сделано с ❤️ и ☕**
